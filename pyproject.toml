[tool.poetry]
name = "koda-validate"
version = "0.1.0"
description = ""
authors = ["Keith Philpott <keith.philpott@icloud.com>"]

[tool.poetry.dependencies]
python = "^3.8"
koda = "0.10.1"

[tool.poetry.dev-dependencies]
pytest = "^7.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
